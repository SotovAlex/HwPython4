1. Метод contains возвращает не верное значение при незаполненном значении символа
    ШАГИ:
        1. Вызвать метод contains и параметрами: ("Hello World", "")
        2. Запустить выполнение
    ОР: возвращается ответ False
    ФР: возвращается ответ True

2. Метод start_with возвращает не верное значение при незаполненном значении символа
    ШАГИ:
        1. Вызвать метод start_with и параметрами: ("Hello World", "")
        2. Запустить выполнение
    ОР: возвращается ответ False
    ФР: возвращается ответ True

3. Метод end_with возвращает не верное значение при незаполненном значении символа
    ШАГИ:
        1. Вызвать метод end_with и параметрами: ("Hello World", "")
        2. Запустить выполнение
    ОР: возвращается ответ False
    ФР: возвращается ответ True